from books_recommender.pipeline.training_pipeline import TrainingPipeline

obj = TrainingPipeline()
obj.start_training_pipeline()

 # so the raw data is created in that we have the zip file and then the zip file is extracted in placed inside the ingested folder. So th efirst data ingestion is done. This is called ml pipeline