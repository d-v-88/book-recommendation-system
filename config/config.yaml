artifacts_config:
  artifacts_dir: artifacts

data_ingestion_config:
  # This is my data ingestion as an input and i have stored the data in a github folder and extracted from there.

  #so this is the input thats a zip file and then what will the output be 3 files those are unzipped files and that will be stored in the artifacts

  # then in the artifacts i will create 3 files dataset, ingested data and raw data

  # In the dataset the csv files will be stored
  # In ingested data
  # In raw data it will download the zip file
  dataset_download_url: https://github.com/entbappy/Branching-tutorial/raw/master/books_data.zip
  dataset_dir: dataset
  ingested_dir: ingested_data
  raw_data_dir: raw_data

  # Here i have used modular coding again if i need to chnage any thing in the future

  # so the raw data is created in that we have the zip file and then the zip file is extracted in placed inside the ingested folder. So th efirst data ingestion is done

  ##--------------------- DATA VALIDATION -----------------------------##
  ## so i want these all things so i have updated it
data_validation_config:
  clean_data_dir: clean_data
  serialized_objects_dir: serialized_objects
  books_csv_file: BX-Books.csv
  ratings_csv_file: BX-Book-Ratings.csv

  #---------------- DATA TRANSFORMATION ----------------#
data_transformation_config:
  transformed_data_dir: transformed_data

model_trainer_config:
  trained_model_dir: trained_model
  trained_model_name: model.pkl

recommendation_config:
  poster_api_url: https://api.themoviedb.org/3/movie/{}?api_key=8265bd1679663a7ea12ac168da84d2e8&language=en-US
